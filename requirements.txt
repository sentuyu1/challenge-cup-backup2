# 挑战杯 XH-202627 — 数学智能体（LangGraph 版）依赖
# 安装：pip install -r requirements.txt

# ── 图编排框架 ──
langgraph>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0

# ── 数学计算 ──
sympy>=1.12
mpmath>=1.3,<1.4
numpy>=1.24.0
scipy>=1.10.0

# ── 数据处理 ──
pandas>=2.0.0

# ── HTTP / 通用 ──
requests>=2.31.0
typing-extensions>=4.9.0
python-dotenv>=1.0.0

# ── 可选：答案相似度（无 Levenshtein 时回退到纯 Python 兜底）──
python-Levenshtein>=0.20.0
